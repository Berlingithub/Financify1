# Used when building for Vercel/production (npm run build)
# Set your Render (or other) backend URL — also add this in Vercel → Settings → Environment Variables
REACT_APP_API_URL=https://YOUR-BACKEND.onrender.com
